# ramnog 0.2.0

* Prepared for CRAN submission.
* Clarified package role as a documentation hub for the 'chef' ecosystem.
* Replaced internal `dummy_fn()` with standard package-level documentation.
* Replaced `testr` dependency with `withr` in tests and vignettes.
